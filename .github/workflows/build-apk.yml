name: Gerar APK Android Nativo

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  build-apk:
    name: Build APK nativo com app embutido
    runs-on: ubuntu-latest

    steps:
      - name: Baixar código
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Configurar Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Instalar dependências
        run: npm install --registry=https://registry.npmjs.org/

      - name: Validar TypeScript
        run: npm run typecheck

      - name: Rodar testes do motor
        run: npm run test:all

      - name: Preparar exportação estática para APK
        run: |
          rm -rf out
          if [ -d src/app/api ]; then
            mv src/app/api src/app/_api_desativada_no_apk
          fi
          if [ -f middleware.ts ]; then
            mv middleware.ts middleware.desativado-no-apk.ts
          fi

      - name: Gerar arquivos internos do APK
        env:
          BUILDMASTER_ANDROID_STATIC: '1'
          NEXT_PUBLIC_BUILDMASTER_CLOUD_API_URL: ${{ vars.BUILDMASTER_CLOUD_API_URL || 'https://buildmaster-ai-git-main-buildmaster-ai.vercel.app/api/cloud/fichas' }}
        run: npm run build

      - name: Adicionar Android se ainda não existir
        run: |
          if [ ! -d android ]; then
            npx cap add android
          fi

      - name: Sincronizar arquivos embutidos no Android
        run: npx cap sync android

      - name: Gerar APK debug
        run: cd android && ./gradlew assembleDebug

      - name: Enviar APK como artefato
        uses: actions/upload-artifact@v4
        with:
          name: BuildMaster-Elite-Tatico-v24-9-apk-nativo
          path: android/app/build/outputs/apk/debug/app-debug.apk
